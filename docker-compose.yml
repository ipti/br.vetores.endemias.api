version: "3.8"

services:
        app:
            build: .
            command: adonis serve
            ports:
                - target: 3333
                  published: 3333
                  protocol: tcp
                  mode: host
            volumes:
                - .:/usr/app
                